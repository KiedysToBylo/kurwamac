   <div class="mt-3 mb-2 d-flex">
       <input type="text" 
       class="form-control" 
       value="<%= query.q %>" 
       placeholder="Szukaj..." 
       name="q">
        <button class="btn btn-primary ms-2">Szukaj</button>
    </div>

<% if (query.q) { %>
    <p class="h5 mb3">Wyniki wyszukiwania dla frazy "<%= query.q %>":</p>
    <% } %>